package nl.thyself.model;

/**
 * Handles requests to the web server.
 */
public class ServerInterface {
  
  /**
   * Synchronizes the most recent data between the
   * app and the server.
   * @return If synchronization succeeded
   */
  public boolean sync() {
    // TODO: method stub
    return false;
  }
}
